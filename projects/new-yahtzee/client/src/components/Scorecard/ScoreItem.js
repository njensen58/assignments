import React from 'react'
import { connect } from 'react-redux';

const ScoreItem = props => {
    return (
        <div>
            ScoreItem
        </div>
    )
}

export default connect(state=>state, {})(ScoreItem)