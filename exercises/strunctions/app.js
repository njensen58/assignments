// Implement all of the listed String Methods below using at least 3 unique Functions.
//
// String Methods
//
// .concat()  = adds to arrays together
// .indexOf() = returns the index of the first occurance
// .lastIndexOf() = returns the index of the final occurance
// .match() = Searches a str for a match against a regEx, returns matches as array object.
//      if regEx does not contain 'g', it will only return first match. null if none.
// .replace(a, b) = Returns a new string with 'a' replaced with 'b'.
// .slice(a, b) = Returns a string starting at index 'a', up to but not including index 'b'.
// .split() = Turns string in to an array of strings. Default is 'a','b' if no arg provided.
// .toLowerCase() = Turns the string to lower case
// .toUpperCase() = Turns the stirng to upper case =
// .substr(a, b) = Extracts beginning at 'a', with 'b' amount of characters.


// 1

var insertInTheMiddle = function(str, insertMe) {
    
}
