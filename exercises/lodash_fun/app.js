const _ = require('lodash');

// _.chunk - Creates an array of elements split into groups
    // syntax ._chunk(arr, [size=n]);
const myArr = [1, 2, 3, 3, 4, 5, 6, 7, 8, 9 ,10];
// console.log(_.chunk(myArr, [size=5]));


// _.times - iteratee is invoked n times returning an array of results each invocation
    // syntax _.times(n, [iteratee=_.identity])

// console.log(_.times(5, function(){
//     console.log('hello');
// }))



// _.map 
