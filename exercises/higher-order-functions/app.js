var purples = ["lavender", "violet", "byzantine", "fusia", "rebecca purple", "barney", "nerple"];

// 1. .forEach();
    purples.forEach(function(purple){
        console.log(purple)
    });
        // This outputs each purple in the purples array just like a for loop with console.log.
    purples.forEach(function(purple, i, arr){

    })


    // These all return a boolean.

// .some();
// .every();
// .filter();
// .find();
// .findIndex();


// 2. .some();
